[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "grit"
version = "0.3.5"
description = "Genome curation pipeline CLI"
requires-python = ">=3.10"
dependencies = [
    "biopython>=1.80",
    "pymysql>=1.0",
    "pyyaml>=6.0",
    "requests>=2.28",
    "rich-click>=1.8.0",
    "rename-and-orient>=1.2.1",
]

[tool.uv.sources]
rename-and-orient = { git = "https://github.com/zilov/rename-and-orient" }

[tool.uv]
package = true

[project.scripts]
grit = "grit.core.click_cli:cli"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.3",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
