# Your Sanger / farm username (used to derive the working directory path)
username: {username}

# NFS path where PretextView maps live before curation starts
pretext_maps_nfs: /nfs/treeoflife-01/teams/grit/data/pretext_maps/

# NFS path where the curator saves the finished PretextView session
curated_pretext_maps_nfs: /nfs/treeoflife-01/teams/grit/data/curated_pretext_maps/

# Hostname of the farm submission node (used for SSH / bsub)
farm_host: tol22-head2

# Absolute path to the directory that contains GritJiraIssue.py
# (the module used to fetch ticket YAML from Jira)
# Temporarily pinned to a known-working copy while the shared version at
# /software/grit/projects/vgp_submission/modules is mid-refactor (new "src"
# package layout not yet resolvable via this path). Revert once that
# refactor lands.
gritjiraissue_path: /nfs/users/nfs_d/dz11/gitlab/vgp_submission/modules/
